import Layout from 'renderer/components/layout';
import PageCard from 'renderer/components/page-card/page-card';
import Report from 'renderer/components/report/report';

const Relatorio = () => {
  return (
    <Layout>
      <PageCard />
      <Report />
    </Layout>
  );
};

export default Relatorio;
