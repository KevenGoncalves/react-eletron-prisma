import Layout from 'renderer/components/layout';
import PageCard from 'renderer/components/page-card/page-card';
import Tables from 'renderer/components/tables/tables';

const Index = () => {
  return (
    <Layout>
      <PageCard />
      <Tables />
    </Layout>
  );
};

export default Index;
